
let constAnswer = prompt('cuando es 2 + 2 ?');
let convertData = parseInt(constAnswer);

if (convertData ===4) {
    alert('Felicitaciones, respuesta correcta!');
} else {
    alert('Error en la respuesta.Intentelo de nuevo');
}